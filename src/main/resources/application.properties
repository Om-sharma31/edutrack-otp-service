server.port=${PORT:8080}

edutrack.cors.allowed-origins=${CORS_ORIGINS:http://127.0.0.1:5500,http://localhost:5500,http://localhost:3000,null,*}

edutrack.otp.expiry-minutes=10
edutrack.otp.length=6

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=oms744311@gmail.com
spring.mail.password=mgoy vvdz gboc sjpd
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

edutrack.mail.from-name=EduTrack School System

spring.thymeleaf.check-template-location=false

# PostgreSQL Database — uses cloud env vars when deployed, falls back to local
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/edutrack}
spring.datasource.username=${PGUSER:postgres}
spring.datasource.password=${PGPASSWORD:omega31}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect